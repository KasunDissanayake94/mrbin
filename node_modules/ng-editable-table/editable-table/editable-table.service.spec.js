import { TestBed, inject } from '@angular/core/testing';
import { EditableTableService } from './editable-table.service';
describe('EditableTableService', function () {
    beforeEach(function () {
        TestBed.configureTestingModule({
            providers: [EditableTableService]
        });
    });
    it('should ...', inject([EditableTableService], function (service) {
        expect(service).toBeTruthy();
    }));
});
//# sourceMappingURL=editable-table.service.spec.js.map